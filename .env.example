# Copy to .env (never commit). All values optional until the milestone that needs them.
QD_PORT=8700
QD_DB=quantdesk.db
# M3+: POSTGRES_URL=, REDIS_URL=
# M4: QuantConnect -> lean login (interactive)
# M6: IB_USERNAME=, IB_PASSWORD= (paper), BINANCE_TESTNET_KEY=, BINANCE_TESTNET_SECRET=
# M10: DATABENTO_KEY= or POLYGON_KEY=
# M12: ANTHROPIC_API_KEY=, OPENAI_API_KEY=, OLLAMA_URL=http://localhost:11434
# M14: SMTP_HOST=, SMTP_USER=, SMTP_PASS=, TELEGRAM_BOT_TOKEN=
